📘 Task Manager REST API – README

A complete CRUD-based REST API built with Node.js and Express, using a local JSON file as a datastore.
This project demonstrates real-world API building concepts including routes, controllers, modular structure, file-based persistence, and structured JSON responses.

🚀 Features 

✅ 1. Create Tasks (POST /tasks)

Creates a new task with:

id (unique, generated by nanoid)

title

completed (default: false)

createdAt timestamp

✅ 2. Get All Tasks (GET /tasks)

Returns a list of all tasks stored in data/tasks.json.

✅ 3. Get Task by ID (GET /tasks/:id)

Fetch a single task using its unique ID.

✅ 4. Update Task (PUT /tasks/:id)

Update:

title

completed

Only provided fields are updated.

✅ 5. Delete Task (DELETE /tasks/:id)

Removes the task with the given ID from the datastore.
